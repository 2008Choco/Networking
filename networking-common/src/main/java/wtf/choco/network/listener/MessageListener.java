package wtf.choco.network.listener;

/**
 * A generic message listener. Acts as a marker interface for message listeners.
 *
 * @see ClientboundMessageListener
 * @see ServerboundMessageListener
 */
public interface MessageListener { }
