package wtf.choco.network.listener;

/**
 * A serverbound message listener. Acts as a marker interface for serverbound message listeners.
 */
public interface ServerboundMessageListener extends MessageListener { }
