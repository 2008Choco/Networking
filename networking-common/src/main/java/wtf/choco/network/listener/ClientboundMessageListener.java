package wtf.choco.network.listener;

/**
 * A clientbound message listener. Acts as a marker interface for clientbound message listeners.
 */
public interface ClientboundMessageListener extends MessageListener { }
